'use strict';

var chai = require('chai'),
    sinonChai = require('sinon-chai'),
    sinon = require('sinon'),
    mock = sinon.mock,
    expect = chai.expect;
    
describe('<%= moduleSafeName %>()', function () {
    it('does something', function () {
        expect(true).to.equal(false);
    });
});
